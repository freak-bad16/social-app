
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>RTI Simplifier</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  
  <nav class="bg-blue-600 p-4 shadow-md">
    <div class="flex items-center justify-between">
      
      <div class="flex items-center space-x-2">
        <img src="your-logo-url.png" alt="Logo" class="w-10 h-10">
        <span class="text-white text-2xl font-semibold">RTI SHAYAK</span>
      </div>

      
      <div class="space-x-4">
        <a href="/login" class="text-white bg-green-500 px-4 py-2 rounded hover:bg-green-600">Login</a>
        <a href="/signup" class="text-white bg-blue-500 px-4 py-2 rounded hover:bg-blue-600">Sign Up</a>
      </div>
    </div>
  </nav>

  
  <div class="bg-gray-200 p-3 shadow-md">
    <div class="flex justify-center space-x-6">
      <a href="/" class="text-blue-600 hover:text-blue-800">Home</a>
      <a href="/articles" class="text-blue-600 hover:text-blue-800">Articles</a>
      <a href="/rti-file" class="text-blue-600 hover:text-blue-800">RTI File</a>
      <a href="/chat" class="text-blue-600 hover:text-blue-800">Chat</a>
      <a href="/view-status" class="text-blue-600 hover:text-blue-800">View Status</a>
      <a href="/faq" class="text-blue-600 hover:text-blue-800">FAQ</a>
      <a href="/view-history" class="text-blue-600 hover:text-blue-800">View History</a>
    </div>
  </div>

  
  <div class="max-w-4xl mx-auto my-10 p-8 bg-white shadow-md rounded-lg">
    <h1 class="text-3xl font-bold text-blue-600 mb-6 text-center">User Profile</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      
      
      <div>
        <h2 class="text-xl font-semibold mb-2">Personal Info</h2>
        <p><strong>Name:</strong> <%= user.name %></p>
        <p><strong>Date of Birth:</strong> <%= user.dob.toDateString() %></p>
        <p><strong>Gender:</strong> <%= user.gender %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Phone:</strong> <%= user.phone %></p>
        <p><strong>Aadhar Number:</strong> <%= user.aadharNumber %></p>
        <p><strong>Account Created:</strong> <%= user.createdAt.toDateString() %></p>
      </div>

      
      <div>
        <h2 class="text-xl font-semibold mb-2">Address & Other Info</h2>
        <p><strong>Country:</strong> <%= user.address.country %></p>
        <p><strong>Pin Code:</strong> <%= user.address.pinCode %></p>
        <p><strong>Area Type:</strong> <%= user.address.areaType %></p>
        <p><strong>Poverty Level:</strong> <%= user.povertyLevel %></p>
        <p><strong>Captcha:</strong> <%= user.captcha %></p>
      </div>
    </div>
  </div>

</body>
</html>
